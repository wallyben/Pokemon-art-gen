# ── Core image processing ────────────────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
scikit-image>=0.21.0
scikit-learn>=1.3.0
scipy>=1.11.0

# ── Vector & SVG ─────────────────────────────────────────────────────────────
svgwrite>=1.4.3
pypotrace>=0.3

# ── CLI & dashboard ───────────────────────────────────────────────────────────
click>=8.1.7
streamlit>=1.28.0

# ── Deep learning ─────────────────────────────────────────────────────────────
# diffusers>=0.24.0 required for StableDiffusionXLControlNetPipeline and
# native IP-Adapter support (load_ip_adapter / set_ip_adapter_scale).
torch>=2.0.0
diffusers>=0.24.0
transformers>=4.35.0
accelerate>=0.24.0
safetensors>=0.4.0

# ── ControlNet conditioning ───────────────────────────────────────────────────
# Provides OpenposeDetector and other ControlNet preprocessors.
controlnet-aux>=0.0.7

# ── IP-Adapter (reference image conditioning) ─────────────────────────────────
# The h94/IP-Adapter weights are downloaded via huggingface_hub.
# The ip_adapter package provides helper utilities used by diffusers.
ip-adapter>=0.1.0

# ── Model management ─────────────────────────────────────────────────────────
huggingface_hub>=0.20.0
